import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff6600', 
    },
    secondary: {
      main: '#000000', 
    },
    background: {
      default: '#ffffff', 
    },
  },
});

export default theme;
